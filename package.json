{
  "name": "bits-living-grid",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "BITS digital studio — The Living Grid (GPGPU particle field spike)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "three": "^0.169.0"
  }
}
